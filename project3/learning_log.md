## Overview

I found this project to be super practical and interesting so far because it combined everything we've learned into a real web application. Unlike the previous projects, we weren't just using the internal terminal, rather we were able to use Flask to create a web browser that users can interact on (html/css). 

The main areas I wanted to improve on for this project was workign with APIs, understanding how data flows between different parts of a program, and learning how Flask connects python to a front-end interface.

## What I Did Well

One thing I did well was breaking the project into clear steps and following it: place names, then coordinates, then nearest stop. understanding this flow early made the coding much easier and helped me debug issues when something was not working.

I also improved working with APIs. I was able to successfully use both the Mapbox Geocoding API and the MBTA API, extracting only the information that I needed. This was something I was not super comfortable with at first, but I got better throiugh testing and iteration (with the assistance of AI)

Also, I did well on seperating logic into different functiosn and files. Keeping mbta_helper.py (the internal function) seperate from app.py (the flask app) made the code more organized and easier to understand, which I hope to use the same structure for my file. 

I was also able to build a working web application using Flask. Also using AI to help assist with simple HTML & CSS code to make the design a lot better.

## What I Plan to Continue for this Project

If I had more time, I would definitely want to improve on the web feeling more like an app. One area specifically would be working on the UI/UX side, enhancing it would make it feel more real and more practical. 

I would also like to expand the functionality by adding features like real time train arrival times or filtering by specific MBTA line (red, green, commuter, etc.). 

Also I want to further explore integrating maps more, like adding more interactive elements (for ex: showing the quickest way to reach the stop or all routes, just like u can on google maps, etc.) or allowing users to explore nearby stops beyond just the closet one.